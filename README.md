# fake_news_Headline_generator
A Python-based command-line application that generates random and humorous fake news headlines by combining predefined subjects, actions, and places. Built using Python's `random` module, it demonstrates core programming concepts such as lists, loops, conditional statements, and user input handling.
